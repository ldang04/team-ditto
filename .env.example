# Supabase Configuration
# Get these from your Supabase project settings: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://your-project-id.supabase.co
SUPABASE_SERVICE_KEY=your-service-role-key-here

# Google Cloud Vertex AI Configuration
# Your GCP project ID where Vertex AI is enabled
GCP_PROJECT_ID=your-gcp-project-id

# Optional: Vertex AI model to use (defaults to gemini-2.5-flash-lite if not set)
VERTEX_MODEL_TEXT=gemini-2.5-flash-lite

# Server Configuration
# Optional: Port number (defaults to 3000 if not set)
PORT=3000
